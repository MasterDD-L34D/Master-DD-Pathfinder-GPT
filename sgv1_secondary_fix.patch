diff --git a/src/data/modules/minmax_builder.txt b/src/data/modules/minmax_builder.txt
index d225d13..2b35eda 100644
--- a/src/data/modules/minmax_builder.txt
+++ b/src/data/modules/minmax_builder.txt
@@ -310,23 +310,37 @@ helpers:
   render_source_badge:
     params:
       - level
-    logic: "mapping = {\"RAW_CANON\":\"[RAW]\",\"PFS\":\"[PFS]\",\"HR\":\"[HR]\",\"META_EXPERT\":\"\U0001F3DB️\",\"META_CORE\":\"\U0001F3DB️\",\"META_COMMUNITY\":\"\U0001F4D6\",\"META_LIVE\":\"\U0001F4AC\"}\nL = (level or \"\").upper()\nreturn mapping.get(L,\"❓\")\n"
+    logic: |
+      mapping = {
+        "RAW_CANON": "[RAW]",
+        "PFS": "[PFS]",
+        "HR": "[HR]",
+        "META_OFFICIAL": "🏛️",
+        "META_EXPERT": "🏛️",
+        "META_CORE": "🏛️",
+        "META_GUIDE": "📘",
+        "META_COMMUNITY": "📚",
+        "META_LIVE": "💬",
+      }
+      L = (level or "").upper()
+      return mapping.get(L, "❓")
   source_level_rank:
     params:
       - level
     logic: >
       order =
-      {'RAW_CANON':1,'PFS':2,'META_EXPERT':3,'META_CORE':4,'META_COMMUNITY':5,'META_LIVE':6,'HR':7}
+      {'RAW_CANON':1,'PFS':2,'META_OFFICIAL':3,'META_EXPERT':4,'META_CORE':5,'META_GUIDE':6,'META_COMMUNITY':7,'META_LIVE':8,'HR':9}
 
-      return order.get((level or 'META_COMMUNITY').upper(), 5)
+      # Default conservativo: se il livello è ignoto trattalo come COMMUNITY.
+      return order.get((level or 'META_COMMUNITY').upper(), order['META_COMMUNITY'])
   sort_meta_by_level:
     params:
       - sources
     logic: |
       def rank(s):
-        lvl = (s.get("level") or "META_LIVE").upper()
-        order = {'RAW_CANON':1,'PFS':2,'META_EXPERT':3,'META_CORE':4,'META_COMMUNITY':5,'META_LIVE':6,'HR':7}
-        return order.get(lvl, 9)
+        lvl = (s.get("level") or "META_COMMUNITY").upper()
+        order = {'RAW_CANON':1,'PFS':2,'META_OFFICIAL':3,'META_EXPERT':4,'META_CORE':5,'META_GUIDE':6,'META_COMMUNITY':7,'META_LIVE':8,'HR':9}
+        return order.get(lvl, 99)
       return sorted(sources or [], key=lambda s: (rank(s), s.get("tipo","~")))
   lookup_meta_badges:
     params:
@@ -445,6 +459,31 @@ helpers:
       p = (provenienza or 'COMMUNITY').upper()
       if p not in ['PAIZO','COMMUNITY','3PP']: p = 'COMMUNITY'
       return f"[{p}]"
+
+  normalize_meta_level:
+    params:
+      - level
+      - provenienza
+    logic: |
+      # Normalizza il livello di una fonte META.
+      # Ammessi: META_OFFICIAL, META_EXPERT, META_CORE, META_GUIDE, META_COMMUNITY, META_LIVE
+      L = (level or "").strip().upper().replace("-", "_")
+      allowed = {
+        "META_OFFICIAL",
+        "META_EXPERT",
+        "META_CORE",
+        "META_GUIDE",
+        "META_COMMUNITY",
+        "META_LIVE",
+      }
+      if L in allowed:
+        return L
+
+      # Fallback: mappa provenienza → level (conservativo)
+      p = (provenienza or "COMMUNITY").strip().upper()
+      if p == "PAIZO":
+        return "META_OFFICIAL"
+      return "META_COMMUNITY"
   get_defense_metric:
     params: []
     logic: |
@@ -625,10 +664,35 @@ helpers:
           return True
       return False
 
+  raw_anchor_detected:
+    params: []
+    logic: |
+      # Heuristica: considera "ancorato" se esistono riferimenti ufficiali in build_state.fonti
+      # o se una fonte in fonti_meta punta a AoN/Paizo.
+      def _is_official_url(u):
+        s = str(u or "").lower()
+        return ("aonprd.com" in s) or ("paizo.com" in s) or ("archivesofnethys" in s)
+
+      for f in (build_state.get("fonti") or []):
+        if isinstance(f, str) and _is_official_url(f):
+          return True
+        if isinstance(f, dict) and _is_official_url(f.get("url") or f.get("link") or f.get("source_url")):
+          return True
+
+      for m in (build_state.get("fonti_meta") or []):
+        if _is_official_url((m or {}).get("link")):
+          return True
+      return False
+
+  raw_anchor_mark:
+    params: []
+    logic: |
+      return "✔" if raw_anchor_detected() else "⏳"
+
   meta_governance_breadcrumb:
     params: []
     logic: |
-      return "🔍 META-SEARCH → 📖 RAW check ✔ → 🧠 META-ANALYSIS → VERDETTO" if any_meta_source() else ""
+      return f"🔍 META-SEARCH → 📖 RAW check {raw_anchor_mark()} → 🧠 META-ANALYSIS → VERDETTO" if any_meta_source() else ""
 
   meta_governance_source_verdict:
     params: []
@@ -669,7 +733,9 @@ helpers:
       if not any_meta_source():
         return ""
       verdict = meta_governance_source_verdict()
-      return meta_governance_breadcrumb() + "\n" + "**Verdetto fonti META:** " + verdict
+      mark = raw_anchor_mark()
+      raw_note = "OK" if mark == "✔" else "PENDING (STEP 0 required)"
+      return meta_governance_breadcrumb() + "\n" + "**Verdetto fonti META:** " + verdict + "\n" + f"**RAW anchoring:** {raw_note}"
   calc_init_mod:
     params:
       - DEX
@@ -1230,6 +1296,7 @@ commands:
       - descrizione
       - tipo
       - provenienza
+      - level
     action:
       - if: '{{ link in [f.link for f in build_state.fonti_meta] }}'
         then:
@@ -1237,14 +1304,18 @@ commands:
         else:
           - set: local.tag
             value: '{{format_meta_tag(provenienza)}}'
+          - set: local.level
+            value: '{{normalize_meta_level(level, provenienza)}}'
+          - set: local.badge
+            value: '{{render_source_badge(local.level)}}'
           - append: build_state.fonti_meta
             value:
               link: '{{link}}'
               descrizione: '{{descrizione}}'
               tipo: '{{tipo}}'
               tag: '{{local.tag}}'
-              level: META_COMMUNITY
-              badge: "\U0001F4D6"
+              level: '{{local.level}}'
+              badge: '{{local.badge}}'
           - output: 'Fonte meta aggiunta: {{local.tag}} {{link}}'
   /add_spell:
     description: Aggiunge incantesimi a un livello di magia
diff --git a/src/data/modules/scheda_pg_markdown_template.md b/src/data/modules/scheda_pg_markdown_template.md
index 053a2d3..009b47c 100644
--- a/src/data/modules/scheda_pg_markdown_template.md
+++ b/src/data/modules/scheda_pg_markdown_template.md
@@ -498,14 +498,36 @@ _Nessuna tabella incantesimi disponibile._
 
 ---
 {# # BEGIN SOURCE_GOVERNANCE_V1 #}
-{% set ns = namespace(has_meta=false) %}
+{# META breadcrumb: mostra solo se esistono fonti META; RAW check dinamico (✔/⏳) #}
+{% set ns = namespace(has_meta=false, has_raw=false) %}
+
+{# 1) Detect META sources (preferisci 'level' META_*) + detect RAW anchors in meta links #}
 {% for f in (fonti_meta or []) %}
-  {% if f.tipo and f.tipo.startswith('META') %}
+  {% if f.level and f.level.startswith('META_') %}
+    {% set ns.has_meta = true %}
+  {% elif f.tipo and f.tipo.startswith('META') %}
     {% set ns.has_meta = true %}
   {% endif %}
+
+  {% if f.link and ('aonprd.com' in (f.link|lower) or 'paizo.com' in (f.link|lower) or 'archivesofnethys' in (f.link|lower)) %}
+    {% set ns.has_raw = true %}
+  {% endif %}
 {% endfor %}
+
+{# 2) Detect RAW anchors in 'fonti' (stringhe o dict) #}
+{% for f in (fonti or []) %}
+  {% if f is string and ('aonprd.com' in (f|lower) or 'paizo.com' in (f|lower) or 'archivesofnethys' in (f|lower)) %}
+    {% set ns.has_raw = true %}
+  {% elif f is mapping %}
+    {% set _u = f.url or f.link or f.source_url %}
+    {% if _u and ('aonprd.com' in (_u|lower) or 'paizo.com' in (_u|lower) or 'archivesofnethys' in (_u|lower)) %}
+      {% set ns.has_raw = true %}
+    {% endif %}
+  {% endif %}
+{% endfor %}
+
 {% if ns.has_meta %}
-> 🔍 META-SEARCH → 📖 RAW check ✔ → 🧠 META-ANALYSIS → VERDETTO
+> 🔍 META-SEARCH → 📖 RAW check {{ '✔' if ns.has_raw else '⏳' }} → 🧠 META-ANALYSIS → VERDETTO
 {% endif %}
 {# # END SOURCE_GOVERNANCE_V1 #}
 > 📎 Fonti Meta (badge sintetico): {{ lookup_meta_badges('any') or '—' }}
diff --git a/src/modules/minmax_builder.txt b/src/modules/minmax_builder.txt
index 3fa97bb..39dd7b6 100644
--- a/src/modules/minmax_builder.txt
+++ b/src/modules/minmax_builder.txt
@@ -311,22 +311,36 @@ helpers:
   render_source_badge:
     params:
       - level
-    logic: "mapping = {\"RAW_CANON\":\"[RAW]\",\"PFS\":\"[PFS]\",\"HR\":\"[HR]\",\"META_EXPERT\":\"\U0001F3DB️\",\"META_CORE\":\"\U0001F3DB️\",\"META_COMMUNITY\":\"\U0001F4D6\",\"META_LIVE\":\"\U0001F4AC\"}\nL = (level or \"\").upper()\nreturn mapping.get(L,\"❓\")\n"
+    logic: |
+      mapping = {
+        "RAW_CANON": "[RAW]",
+        "PFS": "[PFS]",
+        "HR": "[HR]",
+        "META_OFFICIAL": "🏛️",
+        "META_EXPERT": "🏛️",
+        "META_CORE": "🏛️",
+        "META_GUIDE": "📘",
+        "META_COMMUNITY": "📚",
+        "META_LIVE": "💬",
+      }
+      L = (level or "").upper()
+      return mapping.get(L, "❓")
   source_level_rank:
     params:
       - level
     logic: >
       order =
-      {'RAW_CANON':1,'PFS':2,'META_EXPERT':3,'META_CORE':4,'META_COMMUNITY':5,'META_LIVE':6,'HR':7}
+      {'RAW_CANON':1,'PFS':2,'META_OFFICIAL':3,'META_EXPERT':4,'META_CORE':5,'META_GUIDE':6,'META_COMMUNITY':7,'META_LIVE':8,'HR':9}
 
-      return order.get((level or 'META_COMMUNITY').upper(), 5)
+      # Default conservativo: livelli sconosciuti = META_COMMUNITY
+      return order.get((level or 'META_COMMUNITY').upper(), order['META_COMMUNITY'])
   sort_meta_by_level:
     params:
       - sources
     logic: |
       def rank(s):
-        lvl = (s.get("level") or "META_LIVE").upper()
-        order = {'RAW_CANON':1,'PFS':2,'META_EXPERT':3,'META_CORE':4,'META_COMMUNITY':5,'META_LIVE':6,'HR':7}
+        lvl = (s.get("level") or "META_COMMUNITY").upper()
+        order = {'RAW_CANON':1,'PFS':2,'META_OFFICIAL':3,'META_EXPERT':4,'META_CORE':5,'META_GUIDE':6,'META_COMMUNITY':7,'META_LIVE':8,'HR':9}
         return order.get(lvl, 9)
       return sorted(sources or [], key=lambda s: (rank(s), s.get("tipo","~")))
   lookup_meta_badges:
@@ -446,6 +460,24 @@ helpers:
       p = (provenienza or 'COMMUNITY').upper()
       if p not in ['PAIZO','COMMUNITY','3PP']: p = 'COMMUNITY'
       return f"[{p}]"
+
+  normalize_meta_level:
+    params:
+      - level
+      - provenienza
+    logic: |
+      # Normalizza il livello di una fonte META.
+      # Ammessi: META_OFFICIAL, META_EXPERT, META_CORE, META_GUIDE, META_COMMUNITY, META_LIVE
+      L = (level or "").strip().upper().replace("-", "_")
+      allowed = {"META_OFFICIAL","META_EXPERT","META_CORE","META_GUIDE","META_COMMUNITY","META_LIVE"}
+      if L in allowed:
+        return L
+
+      # Fallback: mappa provenienza → level (conservativo)
+      p = (provenienza or "COMMUNITY").strip().upper()
+      if p == "PAIZO":
+        return "META_OFFICIAL"
+      return "META_COMMUNITY"
   get_defense_metric:
     params: []
     logic: |
@@ -636,10 +668,35 @@ helpers:
           return True
       return False
 
+  raw_anchor_detected:
+    params: []
+    logic: |
+      # Heuristica: considera ancorato se esistono riferimenti ufficiali in build_state.fonti
+      # o se una fonte in fonti_meta punta a AoN/Paizo.
+      def _is_official_url(u):
+        s = str(u or "").lower()
+        return ("aonprd.com" in s) or ("paizo.com" in s) or ("archivesofnethys" in s)
+
+      for f in (build_state.get("fonti") or []):
+        if isinstance(f, str) and _is_official_url(f):
+          return True
+        if isinstance(f, dict) and _is_official_url(f.get("url") or f.get("link") or f.get("source_url")):
+          return True
+
+      for m in (build_state.get("fonti_meta") or []):
+        if _is_official_url((m or {}).get("link")):
+          return True
+      return False
+
+  raw_anchor_mark:
+    params: []
+    logic: |
+      return "✔" if raw_anchor_detected() else "⏳"
+
   meta_governance_breadcrumb:
     params: []
     logic: |
-      return "🔍 META-SEARCH → 📖 RAW check ✔ → 🧠 META-ANALYSIS → VERDETTO" if any_meta_source() else ""
+      return f"🔍 META-SEARCH → 📖 RAW check {raw_anchor_mark()} → 🧠 META-ANALYSIS → VERDETTO" if any_meta_source() else ""
 
   meta_governance_source_verdict:
     params: []
@@ -680,7 +737,16 @@ helpers:
       if not any_meta_source():
         return ""
       verdict = meta_governance_source_verdict()
-      return meta_governance_breadcrumb() + "\n" + "**Verdetto fonti META:** " + verdict
+      mark = raw_anchor_mark()
+      raw_note = "OK" if mark == "✔" else "PENDING (STEP 0 required)"
+      return (
+        meta_governance_breadcrumb()
+        + "\n"
+        + "**Verdetto fonti META:** "
+        + verdict
+        + "\n"
+        + f"**RAW anchoring:** {raw_note}"
+      )
   calc_init_mod:
     params:
       - DEX
@@ -1242,6 +1308,7 @@ commands:
       - descrizione
       - tipo
       - provenienza
+      - level
     action:
       - if: '{{ link in [f.link for f in build_state.fonti_meta] }}'
         then:
@@ -1249,14 +1316,18 @@ commands:
         else:
           - set: local.tag
             value: '{{format_meta_tag(provenienza)}}'
+          - set: local.level
+            value: '{{normalize_meta_level(level, provenienza)}}'
+          - set: local.badge
+            value: '{{render_source_badge(local.level)}}'
           - append: build_state.fonti_meta
             value:
               link: '{{link}}'
               descrizione: '{{descrizione}}'
               tipo: '{{tipo}}'
               tag: '{{local.tag}}'
-              level: META_COMMUNITY
-              badge: "\U0001F4D6"
+              level: '{{local.level}}'
+              badge: '{{local.badge}}'
           - output: 'Fonte meta aggiunta: {{local.tag}} {{link}}'
   /add_spell:
     description: Aggiunge incantesimi a un livello di magia
diff --git a/src/modules/scheda_pg_markdown_template.md b/src/modules/scheda_pg_markdown_template.md
index 7531f9c..2c1a112 100644
--- a/src/modules/scheda_pg_markdown_template.md
+++ b/src/modules/scheda_pg_markdown_template.md
@@ -578,14 +578,36 @@ _Nessuna tabella incantesimi disponibile._
 
 ---
 {# # BEGIN SOURCE_GOVERNANCE_V1 #}
-{% set ns = namespace(has_meta=false) %}
+{# META breadcrumb: mostra solo se esistono fonti META; RAW check dinamico (✔/⏳) #}
+{% set ns = namespace(has_meta=false, has_raw=false) %}
+
+{# 1) Detect META sources (preferisci 'level' META_*) + detect RAW anchors in meta links #}
 {% for f in (fonti_meta or []) %}
-  {% if f.tipo and f.tipo.startswith('META') %}
+  {% if f.level and f.level.startswith('META_') %}
+    {% set ns.has_meta = true %}
+  {% elif f.tipo and f.tipo.startswith('META') %}
     {% set ns.has_meta = true %}
   {% endif %}
+
+  {% if f.link and ('aonprd.com' in (f.link|lower) or 'paizo.com' in (f.link|lower) or 'archivesofnethys' in (f.link|lower)) %}
+    {% set ns.has_raw = true %}
+  {% endif %}
 {% endfor %}
+
+{# 2) Detect RAW anchors in 'fonti' (stringhe o dict) #}
+{% for f in (fonti or []) %}
+  {% if f is string and ('aonprd.com' in (f|lower) or 'paizo.com' in (f|lower) or 'archivesofnethys' in (f|lower)) %}
+    {% set ns.has_raw = true %}
+  {% elif f is mapping %}
+    {% set _u = f.url or f.link or f.source_url %}
+    {% if _u and ('aonprd.com' in (_u|lower) or 'paizo.com' in (_u|lower) or 'archivesofnethys' in (_u|lower)) %}
+      {% set ns.has_raw = true %}
+    {% endif %}
+  {% endif %}
+{% endfor %}
+
 {% if ns.has_meta %}
-> 🔍 META-SEARCH → 📖 RAW check ✔ → 🧠 META-ANALYSIS → VERDETTO
+> 🔍 META-SEARCH → 📖 RAW check {{ '✔' if ns.has_raw else '⏳' }} → 🧠 META-ANALYSIS → VERDETTO
 {% endif %}
 {# # END SOURCE_GOVERNANCE_V1 #}
 > 📎 Fonti Meta (badge sintetico): {{ lookup_meta_badges('any') or '—' }}
